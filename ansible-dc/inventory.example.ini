# Inventario de ejemplo. Copialo a inventory.ini y ajusta a tu entorno.
# El modulo usa samba-tool LOCAL, asi que Ansible debe ejecutarse en el propio DC
# (o via connection local sobre el host del DC).

[domain_controllers]
dc1 ansible_host=192.0.2.10 ansible_connection=ssh ansible_user=adminlinux

[domain_controllers:vars]
# Recomendado: cargar usuarios/grupos desde group_vars + Ansible Vault.
